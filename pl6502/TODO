Branch feature/split_counters:
- Fix load signal for counter_updown
- Create simple counter (161)
- Replace STEP + PHASE counters for simple counters

Branch:
- Combine STEP and PHASE in a single (double) counters

Branch:
- Implement STEP_clr at the CU level to shortcut the instruction cycle